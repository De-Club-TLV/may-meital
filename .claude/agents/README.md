# Project Agents

This folder holds agents scoped to the May Meital form project.

- New agents require **Yuval's explicit approval**.
- Only create one when Dasha's general agents (`~/.claude/agents/`) genuinely don't cover the need.
- Keep agents narrow and purpose-built. If the same job could be done with a general agent + a clear prompt, skip the new agent.
